{
  "name": "nu-lambda",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && sls -v deploy",
    "undeploy": "sls -v remove"
  },
  "author": "rinse",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.6",
    "typescript": "^4.0.5"
  }
}
